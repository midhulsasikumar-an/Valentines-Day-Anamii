This folder holds images and other media used by the `index.html` birthday design.

- Replace `screen.svg` with the provided screenshot named `screen.png` if you have it.
- If you want an actual PNG with the screenshot, add `screen.png` to this folder and update the `<img>` source in `index.html` to `assets/screen.png`.
